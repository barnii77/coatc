// this is a comment
fn main(b) {
    let a = 3;
    b = add1(a * 0x3f - 2);
    if (1 - a) {
        return 1;
        0
    } else {
        //-a
    };
    while (b) {
        print(b);
    }
    print(a);
    while {}
    return 0x69;
}

fn add1(x) {
    return x + 1;
}
