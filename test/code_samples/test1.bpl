fn main(b) {
    let a = 3;
    b = add1(a * 3 - 2);
    if (1 - a) {
        return 1;
        0
    } else {
        -a
    };
    while (b) {
        print(b);
    }
    print(a);
    return 0;
}

fn add1(x) {
    return x + 1;
}
